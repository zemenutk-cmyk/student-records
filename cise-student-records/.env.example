# Copy this file to .env and set a real secret before building for production.
# This key encrypts the on-device SQLite database (SQLCipher via
# @capacitor-community/sqlite). Do not commit the real .env file.
VITE_DB_SECRET=change-me-to-a-long-random-string
